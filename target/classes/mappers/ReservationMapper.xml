<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.care.plusAir.booking.repository.PayMentDAO">
	<insert id="insert" parameterType="com.care.plusAir.ibe.dto.ReservationDTO">
		INSERT INTO Reservation VALUES(#{regNo},#{memberNo},#{engLastName},#{engFirstName},#{regDate},#{regType},#{priceTotal},#{couponNo},#{pointUsed},#{paymentDate},#{paymentType},#{paymentNo})
	</insert>
</mapper>
